{
    "$schema": "https://raw.githubusercontent.com/vicinaehq/vicinae/refs/heads/main/extra/schemas/extension.json",
    "name": "ssh",
    "title": "ssh",
    "description": "Open a host in ssh",
    "categories": [],
    "license": "MIT",
    "author": "leiserfg",
    "contributors": [],
    "pastContributors": [],
    "icon": "extension_icon.png",
    "commands": [
        {
            "name": "ssh",
            "title": "SSH",
            "description": "Connect to ssh",
            "mode": "view"
        }
    ],
    "preferences": [
        {
            "name": "executor",
            "title": "SSH executor",
            "description": "Comand used to connect ssh host to",
            "required": false,
            "type": "textfield",
            "default": "kitty -1 kitten ssh"
        }
    ],
    "scripts": {
        "build": "vici build",
        "dev": "vici develop"
    },
    "dependencies": {
        "@vicinae/api": "^0.15.5"
    },
    "devDependencies": {
        "typescript": "^5.9.2"
    }
}
